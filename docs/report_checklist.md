# 報告素材檢查清單

做完每一步時對照，確保該留的都留了。

## 自動產生（不用管，跑程式就有）
- [x] results.csv —— 每個版本的指令數/cycle/組成，跑 ./gemm 自動累積
- [x] 測試輸出 —— test_all（55/55）、test_rvv、正確性驗證

## 每個版本「定版」時要留
- [x] 純量：指令組成表（baseline.md 已存）
- [ ] 向量：跑完截一次指令組成表，存到 docs/
- [ ] 自訂：跑完截一次指令組成表，存到 docs/
- [ ] 三版本各自通過 ref_gemm 正確性驗證的畫面

## 程式碼片段（從 GitHub 對應 commit 抓）
- [ ] 純量內層迴圈（14 條，含位址計算）
- [ ] 向量內層迴圈（對照位址計算怎麼被攤掉）
- [ ] 自訂指令的 C++ 實作 + 使用它的迴圈
- [ ] vsetvli 的實作（講 RVV 長度無關特性時用）

## 圖表（用 results.csv 畫）
- [ ] 三版本指令數 vs N（折線圖）
- [ ] 三版本 cycle 數 vs N
- [ ] 加速比長條圖（純量=1，向量、自訂各多少倍）
- [ ] 指令組成堆疊圖（位址計算佔比：純量 vs 向量 vs 自訂）
- [ ] VLEN 掃描：向量寬度 vs 加速比

## 寫作（不需數據，隨時能寫）
- [ ] 動機、RVV 背景、為什麼選 GEMM
- [ ] 系統架構圖
- [ ] cycle 模型假設 + 限制說明（務必寫，主動說明限制會加分）
- [ ] 結論：加速從哪來、未來延伸（Transformer）
