# 實驗記錄

> 每完成一個版本或做一次實驗，就在這裡記幾行：做了什麼、數字變化、踩到的坑。
> 目的是留下「過程」，寫報告和口試（被問困難怎麼解決）時用得到。

---

## W1 純量基準線（已完成）

- 實作 RV32IM 55 條指令，自動測試 55/55 通過。
- 純量 GEMM 4×4：**1058 條指令，1618 cycle**。
- 關鍵發現：指令組成中**位址計算佔 54.4%**，真正運算僅 12.1%。
- 坑：RV32M 的 mul 用有號乘法會有未定義行為，改用無號 `a*b`（低 32 位結果相同）。

## W2 向量狀態 + vsetvli（已完成）

- 加入 v0~v31、vl、vtype；實作 vsetvli，測試 6/6。
- 驗證核心行為：VLEN=128/SEW=32 → VLMAX=4，AVL 超過時 vl 被截到 4。
- （待補：這裡記你自己實作時遇到的問題）

## W3 向量載入/儲存/加法（待做）

- 目標：vle32.v / vse32.v / vadd.vv
- 指令數：（做完填）
- 坑：（做完填）

## W4 向量版 GEMM（已完成）

- 實作 vmacc.vx（vd += 純量 × 向量），向量測試 12/12。
- 向量化策略：一次算 C 的一整排 4 個元素（VL=4）。
  純量 A 元素廣播，乘上 B 一列連續 4 個，vmacc 一次做 4 組乘加。
- **純量 1058 → 向量 264，指令數降到 1/4（加速 4.0x）**。cycle 加速 4.1x。
- N 越大加速比越穩定在約 4.3x（見下表）。

  | N | 純量指令 | 向量指令 | 指令加速 | cycle加速 |
  |---|--------:|--------:|:-------:|:--------:|
  | 4 | 1058 | 264 | 4.01x | 4.12x |
  | 8 | 7774 | 1840 | 4.22x | 4.31x |
  | 16 | 59702 | 13944 | 4.28x | 4.36x |
  | 32 | 468070 | 108904 | 4.30x | 4.38x |

- 觀察：真正運算的佔比從 12% 降到 7.8%，但**位址計算佔比反而升到 59%**。
  這正是自訂指令（W6）要攻擊的目標——RVV 沒有解決位址計算。
- 坑：向量累加器歸零沒有現成指令，用 vsub.vv v3,v3,v3（自己減自己）實現。

## W6 自訂指令（待做）

- 設計的指令：（填）
- 向量 ? → 自訂 ?：（做完填）

## VLEN 掃描（待做）

- VLEN=128 / 256 / 512 的加速比：（做完填）
